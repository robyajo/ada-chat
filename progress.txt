# Ada Chat FE — Progress Log

## [2026-06-03] Initial setup — Removed direct Patuih SDK usage
- Removed `patuih-sdk` from dependencies
- Created `src/services/api.ts` — REST client with JWT auto-refresh
- Created `src/services/socket.ts` — WebSocket manager (connectChat, joinRoom, sendMessage)
- Refactored `components/Lobby.tsx` — Login/Register flow + API key validation via backend
- Refactored `components/Chat.tsx` — Connect to backend WS instead of direct Patuih Gateway
- Updated `App.tsx` — Added userId to Session interface
- Updated `.env.example` — Simplified to only `VITE_API_URL`
- Passed `npm run typecheck` and `npm run lint` (no new errors in our code)

## [2026-06-03] Auth flow refactor — Login -> Lobby (room selection), not directly to Chat
- Created `components/OAuthCallback.tsx` — handles Google/Discord OAuth redirect from backend
  - Reads accessToken & refreshToken from URL search params
  - Saves tokens via setTokens() to localStorage
  - Redirects to / on success, shows error on failure
- Refactored `App.tsx` — auth state separated from chat session
  - Detects /oauth/callback path -> renders OAuthCallback
  - On mount, checks localStorage for token -> fetches /api/v1/auth/me for authUser
  - Three render states: OAuthCallback / Chat (when session exists) / Lobby
  - saveSession/loadSession helpers for chat_session localStorage
- Refactored `components/Lobby.tsx` — new flow: login -> room selection -> chat
  - New props: authUser, onAuthSuccess, onLogout (replaces standalone auth logic)
  - Auth form shown only when not authenticated
  - After auth success -> stays on Lobby showing Room Selection (Join/Create buttons)
  - Patuih key setup shown after login if user has no key, then back to Room Selection
  - Logout button in Room Selection view
  - Only calls onEnter() when user actually joins or creates a room
- Updated AGENTS.md — structure + auth flow diagram
- Updated SKILL.md — App.tsx pattern, Lobby new state machine, OAuthCallback pattern, Auth section
## [2026-06-03] Fix session expiry — auto-redirect ke login saat token expired
- services/api.ts: clearTokens() sekarang dispatch CustomEvent('auth:expired')
- App.tsx: listen uth:expired → clear authUser + session → user lgsg liat login form


## [2026-06-03] Chat jadi home — FAB, profile, settings, welcome screen
- App.tsx: user dg patuihApiKey langsung ke Chat (welcome screen, bukan Lobby)
- Chat.tsx: dua mode render — in-room (chat biasa) dan welcome (no room)
- Chat.tsx: Floating Action Button (FAB) pojok kanan bawah — New Room, Join Room, Find by PIN
- Chat.tsx: Profile Sheet — avatar (inisial), PIN, username, display name, status API Key
- Chat.tsx: Settings Sheet — update Patuih API Key
- Chat.tsx: Dialog Create Room + Join Room (modal)
- Chat.tsx: Find by PIN — cari user via PIN 6 digit
- Chat.tsx: Header in-room mode skrg ada icon Profile + Settings
- AuthUser interface skrg include patuihApiKey, patuihTenantId
- Updated SKILL.md — App flow + Chat patterns

## [2026-06-03] Room management — sidebar room list, create room with name, room settings
- Chat.tsx: fetch user rooms via GET /api/v1/chat/rooms on mount
- Chat.tsx: handleSelectRoom — klik room di sidebar langsung pindah room
- Chat.tsx: handleOpenRoomSettings — room settings sheet untuk owner (edit nama + manage members)
- Chat.tsx: handleSaveRoomName + handleRemoveMember
- Create Room dialog: tambah input Room Name (wajib), panggil POST /api/v1/chat/rooms

## [2026-06-03] Create Room dialog — emoji avatar, invite by PIN, room name
- Dialog Create Room: tambah emoji picker (12 emoji), input Room Name, Invite Friends by PIN
- handleAddInvite: cari user by PIN, tambah ke invited list (dengan remove button)
- handleCreateRoomApi: kirim avatarUrl, invite semua invitedUsers setelah room dibuat

## [2026-06-03] Refactor Chat.tsx — extract ProfileSheet + SettingsSheet ke com-chat/
- Created src/components/com-chat/ProfileSheet.tsx (default export)
- Created src/components/com-chat/SettingsSheet.tsx (default export)
- Chat.tsx imported them via import ProfileSheet/SettingsSheet
- Chat.tsx shrunk ~60 lines

## [2026-06-03] Hapus semua API Key per-user — pake PATUIH_SYSTEM_API_KEY
- App.tsx: AuthUser dihapus patuihApiKey/patuihTenantId, Session dihapus apiKey/tenantId
- Chat.tsx: AuthUser + Session interface dihapus field API key, semua referensi apiKey/tenantId dibersihkan
- Lobby.tsx: dihapus form input API Key, Patuih setup, chat_key

## [2026-06-03] Database persistence — load messages from backend
- Chat.tsx: tambah useEffect fetch GET /api/v1/chat/messages/:roomId on room change
- Messages dari backend diformat dan di-set ke state messages
